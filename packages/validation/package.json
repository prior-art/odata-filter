{
  "name": "@prior-art/odata-filter-validation",
  "version": "1.0.0",
  "description": "A query filter parser implementing the OData standard",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist node_modules reports coverage",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "jest",
    "test:mutation": "stryker run"
  },
  "devDependencies": {
    "@prior-art/odata-filter-core": "^1.0.0"
  },
  "peerDependencies": {
    "@prior-art/odata-filter-core": "^1.0.0"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}
