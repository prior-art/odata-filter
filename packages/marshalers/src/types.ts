export enum MongoOperators {
  AND = '$and',
  OR = '$or',
  IN = '$in',
  EQ = '$eq',
  NE = '$ne',
  LT = '$lt',
  LTE = '$lte',
  GT = '$gt',
  GTE = '$gte',
  NOT = '$not',
  UNKNOWN = 'unknown',
}
